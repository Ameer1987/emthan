{% extends 'base.html.twig' %}

{% block body %}
    <h1>Subbookcontainertype</h1>

    <table>
        <tbody>
            <tr>
                <th>Item</th>
                <td>{{ subBookContainerType.item }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ subBookContainerType.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('subbookcontainertype_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('subbookcontainertype_edit', { 'id': subBookContainerType.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
