{% extends 'base.html.twig' %}

{% block body %}
    <h1>Subjects list</h1>

    <table>
        <thead>
            <tr>
                <th>Item</th>
                <th>Createdby</th>
                <th>Createdat</th>
                <th>Updatedby</th>
                <th>Updatedat</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for subject in subjects %}
            <tr>
                <td><a href="{{ path('subject_show', { 'id': subject.id }) }}">{{ subject.item }}</a></td>
                <td>{{ subject.createdBy }}</td>
                <td>{% if subject.createdAt %}{{ subject.createdAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ subject.updatedBy }}</td>
                <td>{% if subject.updatedAt %}{{ subject.updatedAt|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ subject.id }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('subject_show', { 'id': subject.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('subject_edit', { 'id': subject.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('subject_new') }}">Create a new subject</a>
        </li>
    </ul>
{% endblock %}
