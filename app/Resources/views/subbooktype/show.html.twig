{% extends 'base.html.twig' %}

{% block body %}
    <h1>Subbooktype</h1>

    <table>
        <tbody>
            <tr>
                <th>Item</th>
                <td>{{ subBookType.item }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ subBookType.id }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('subbooktype_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('subbooktype_edit', { 'id': subBookType.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
